global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: "tf-serving-monitor"

scrape_configs:
  - job_name: wine-quality-tf-serving
    scheme: https
    metrics_path: /monitoring/prometheus/metrics
    static_configs:
      - targets:
          - aqilaziz-wine-quality-tf-serving-production.up.railway.app
